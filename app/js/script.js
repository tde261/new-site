/*window.onload = function() {
  
  var blocks = document.querySelectorAll('.block');

    for( i=0; i<blocks.length; i++) {

      blocks[i].addEventListener('mousedown', function() {

        var initialCursorX = event.clientX;

        this.parentNode.addEventListener('mousemove', function() {

          var currentCursorX = event.clientX;

          var moveX = currentCursorX - initialCursorX;

          var posX = this.offsetLeft;

          var currentLeft = posX + moveX;

          this.style.left = currentLeft + "px";
          
          
          console.log(posX);
        });
      });
    }

  
};


//    console.log();

*/


 


